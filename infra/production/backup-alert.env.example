# Optional. Install as /etc/buildingos/backup-alert.env with root:root mode 0600.
# The executable receives a non-secret JSON event on stdin.
BACKUP_ALERT_HOOK=/opt/pawtech/operations/bin/buildingos-backup-alert-hook

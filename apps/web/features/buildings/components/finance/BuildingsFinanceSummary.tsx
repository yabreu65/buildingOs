'use client';

import Card from '@/shared/components/ui/Card';

/**
 * BuildingsFinanceSummary: Placeholder for buildings-level finance summary
 *
 * Displays aggregated financial metrics per building
 * Currently shows placeholder text - implementation pending
 */
export const BuildingsFinanceSummary = () => {
  return (
    <Card className="p-6">
      <p className="text-muted-foreground">
        Tabla de resumen por edificio (próximamente)
      </p>
    </Card>
  );
};
